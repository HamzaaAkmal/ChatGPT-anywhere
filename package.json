{
  "name": "chatgpt-anywhere",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local OpenAI-compatible chat wrapper with a Chrome extension control panel.",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js",
    "check": "node --check server/index.js && node --check server/lib/security.js && node --check server/lib/store.js"
  },
  "engines": {
    "node": ">=20"
  }
}
